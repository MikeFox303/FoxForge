# FoxForge v0.1.0-alpha.4.2

Second hotfix for `alpha.4`, focused on completing interface stabilization before physical printer validation.

> **Post-publication status — 2026-09-05:** the guarded release workflow completed successfully on exact release commit `fe5b3437f1e342548df74ded78557c771ef40710`, the multi-architecture GHCR image was published, and the companion Umbrel Store package was pinned to the immutable OCI digest. Representative physical validation is still pending. This note updates the current `main` documentation only; it does not move or rewrite the immutable release tag/commit.

## Fixed

- Add Printer now renders in an isolated `document.body` modal layer instead of mixing with sticky/filter stacking contexts.
- Modal backdrop owns the viewport, dialog surfaces remain opaque, and global controls no longer render above the modal.
- Body scrolling is locked while Add Printer is open and restored when it closes; Escape closes the dialog.
- Phone modal geometry respects dynamic viewport and iOS safe-area constraints.
- Operator Access was moved into the responsive topbar flow and no longer covers workspace content.
- Mobile Operator Access hit-testing and stacking were corrected so route content cannot intercept its control.
- Phone, tablet, 16:9 and 32:9 layouts are bounded without uncontrolled horizontal stretching or clipping.
- Russian and Ukrainian locked-write/fallback errors are localized and covered by responsive checks.
- Invalid CSS custom-property usage found during stabilization was corrected.
- Backend request-local aiohttp state now uses typed `RequestKey` storage, removing the warning found during release-readiness validation.

## Regression protection

- Browser Acceptance covers Overview, Printers, Queue, Materials, Inventory, Farm and System.
- Required viewport projects are phone 390x844, tablet 900x1024, desktop 1920x1080 and ultrawide 5120x1440.
- Dedicated Add Printer coverage checks viewport ownership, z-order, opaque surface, scroll lock/recovery and Escape close behavior.
- Dedicated Operator Access coverage checks viewport containment, hit-testing, Add Printer separation and compact close state.
- RU/UK layout coverage is retained for phone and 16:9 desktop.
- Browser Acceptance fails on `pageerror` or `console.error` across primary routes.
- Frontend unit tests reject undefined CSS custom-property references unless an explicit fallback is present.
- The guarded release workflow reruns production-container Browser Acceptance against the exact release commit before creating the immutable Git tag or publishing the semantic GHCR image.

## Validation status

The stabilization PR passed Web UI, Python/printer contracts, deployment authentication, container, security and Browser Acceptance gates. Its final Browser Acceptance evidence contained 66 screenshots covering the supported viewport matrix, RU/UK layouts, Add Printer and Operator Access states and was manually reviewed before merge.

The guarded release workflow then revalidated exact merged release commit `fe5b3437f1e342548df74ded78557c771ef40710` and completed successfully as workflow run `33973431720` before publication. The annotated `v0.1.0-alpha.4.2` tag resolves to that exact release commit.

Automated CI, mocks, QEMU/container smoke tests and browser emulation do **not** constitute physical printer validation. `AUD-003` and `AUD-013` therefore remain `VALIDATION REQUIRED` until reviewed real-device evidence exists.

## Publication status

Published image:

`ghcr.io/mikefox303/foxforge:0.1.0-alpha.4.2`

Immutable multi-architecture OCI index digest:

`sha256:39d2f2fd02ed8dafe68ce741543642a62d9f3669d2deeb118bc5abce61589fc6`

Exact deployable image:

`ghcr.io/mikefox303/foxforge:0.1.0-alpha.4.2@sha256:39d2f2fd02ed8dafe68ce741543642a62d9f3669d2deeb118bc5abce61589fc6`

Published platforms:

- `linux/amd64`;
- `linux/arm64`.

The companion Umbrel Store package is published from Store commit `e842c411e26689609e9bbba4681df903f3624bbd` as `my3d-foxforge` version `0.1.0-alpha.4.2`, pinned to the immutable image above. It preserves `${APP_DATA_DIR}/data:/data` and maps Umbrel `APP_PASSWORD` to `FOXFORGE_COMMAND_TOKEN` for protected writes.

## Physical validation handoff

Physical testing must use the exact Alpha 4.2 Umbrel package/image identity above on:

- Raspberry Pi 5 / Umbrel;
- Bambu Lab X2D + AMS 2 Pro;
- Moonraker/OpenKE / Ender 3 V3 KE.

The canonical handoff is documented in `docs/status/alpha4-fix2-physical-validation-handoff-2026-09-05.md`, the full behavioral matrix in `docs/testing/physical-validation-runbook.md`, and the separate post-release verifier bootstrap in `docs/testing/alpha4.2-validation-tooling-bootstrap.md`.

The post-release validation tooling from PRs #107–#109 is intentionally separate from the released application image. It may be used to collect and verify Alpha 4.2 evidence, while evidence manifest `sourceCommit` remains the immutable released application commit `fe5b3437f1e342548df74ded78557c771ef40710`.

## Upgrade notes

No persistence schema change is introduced by this UI hotfix. Back up `/data` before updating. For canonical validation, install/update only the Store package pinned to the exact Alpha 4.2 OCI digest above; do not substitute `main`, a floating tag, a locally rebuilt image, Alpha 4 or Alpha 4.1.

Open dependency/tooling upgrades are not part of the frozen Alpha 4.2 candidate. If a production code, dependency, packaging, migration, security or printer-transport change becomes mandatory before physical acceptance, prepare a new release identity instead of silently changing Alpha 4.2.

## License

AGPL-3.0-only.
